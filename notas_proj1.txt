Tokens:
    "private" => <PRIVATE : "private">
    Identifier => <ID : [a-zA-Z][a-zA-Z0-9_]*>

    Regras:

    EBNF => VarDecl = Type Identifier ";"
                       || 
                       \/

JavaCC => void VarDecl() : {}
        {
            Type() <ID> <SEMICOLON>
        }



